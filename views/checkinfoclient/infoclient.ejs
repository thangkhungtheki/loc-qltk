<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Client Information</title>
</head>
<body>
    <h1>Client Information</h1>
    <p>IP Address: <%= ip %></p>
    <p>User Agent: <%= userAgent %></p>
    <p>Is Mobile: <%= isMobile %></p>
    <p>Is Desktop: <%= isDesktop %></p>
    <p>Browser: <%= browser %></p>
    <p>Platform: <%= platform %></p>
    <p>Version: <%= version %></p>
    <p>OS: <%= os %></p>

    
    <div class="container mt-5">
        <h1>Google Form</h1>
        <!-- Google Form iframe -->
        <div class="embed-responsive embed-responsive-4by3">
            <iframe class="embed-responsive-item" src="https://docs.google.com/forms/d/e/1FAIpQLScnlkuMNqMwDmQcU9yvKGz8GrHICo8wgCt6CHeudKLy3XpkQw/viewform?embedded=true" width="100%" height="1200px" >Loading…</iframe>
        </div>

        <!-- Button -->
        <button id="submitButton" class="btn btn-primary mt-3">Gửi</button>
    </div>

    <!-- Bootstrap JS (Optional) -->
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

    <script>
        // Function to submit the form
        function submitForm() {
            // Get values from the form (if needed)
            // var value1 = document.getElementById('input1').value;
            // var value2 = document.getElementById('input2').value;

            // Perform any necessary actions before submitting the form
            
            // Simulate form submission
            alert('Form đã được gửi!');
        }

        // Add event listener to the submit button
        document.getElementById('submitButton').addEventListener('click', function() {
            submitForm();
        });
    </script>
    
</body>
<style>
        /* CSS để ẩn nút gửi mặc định của Google Form */
        
    </style>
</html>
